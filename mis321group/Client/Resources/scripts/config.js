/**
 * API base URL — must match the backend.
 * Local: http://localhost:5253 (or https://localhost:7131 for https profile)
 * GitHub Pages: set to your Heroku API root, e.g. https://group14-321-....herokuapp.com
 * Then set Heroku CORS: Cors__AllowedOrigins=https://YOUR.github.io
 */
window.APP_CONFIG = {
  apiBaseUrl: "http://localhost:5253"
};
